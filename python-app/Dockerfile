# Используем базовый образ Python
FROM python:3

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файлы из папки python-app внутрь контейнера
# COPY python-app/main.py .  # Закомментируйте или удалите эту строку

# Определяем команду для запуска приложения
CMD ["python", "main.py"]
